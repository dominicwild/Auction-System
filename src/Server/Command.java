package Server;

/**
 * Enumerations to represent commands the server will conduct in response to
 * command messages sent over the group communication channel.
 *
 * @author DominicWild
 */
public enum Command {
    SYNC;
}
